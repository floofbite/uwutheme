{{#if this.filteredTopics}}
<div class='featured-lists__wrapper above-main-container full-width'>
    <div class='featured-lists__list-container {{@list.classname}}'>
        <div class='featured-lists__list-header'>
          <h2>{{theme-i18n "features_list.latest.status"}}</h2>
          <a href='/latest' class='feed-link'>{{theme-i18n "features_list.latest.all"}}</a>
          <DButton
            @label={{theme-i18n "features_list.latest.new"}}
            @icon='plus'
            @iconPosition='left'
            @onClick={{action "createTopic"}}
            class='btn btn-default'
            />
        </div>
         <TopicList
                @topics={{filteredTopics}}
                @showPosters='true'
                class='featured-lists__list-body'
              />
    </div>
</div>
{{/if}}